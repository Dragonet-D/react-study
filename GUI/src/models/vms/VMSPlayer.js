// import { isSuccess } from 'utils/helpers';
// import { getChannelGroupTreeByFilterApi } from 'api/vms';

export default {
  namespace: 'VMSPlayer',
  state: {
    namespace: 'VMSPlayer'
  },
  effects: {
    // *getChannelListData({ payload }, { call, put }) {
    //   const result = yield call(getChannelGroupTreeByFilterApi, payload.id);
    //   if (isSuccess(result)) {
    //     yield put({
    //       type: 'getChannelListDataSuccess',
    //       listData: result.data
    //     });
    //   }
    // }
  },
  reducers: {
    //   getChannelListDataSuccess(state, { listData }) {
    //     return {
    //       ...state,
    //       listData
    //     };
    //   }
    // }
  }
};
